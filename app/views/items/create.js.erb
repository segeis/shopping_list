<% if @item.new_record? %>
	$('input#item_name').effect('highlight', { color: 'red' });
<% else %>
	$('table#items').append("<%= escape_javascript(render @item) %>");
	$('table<%= dom_id(@item) %>').effect('highlight');
<% end %> 